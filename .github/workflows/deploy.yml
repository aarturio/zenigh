name: Deploy to AWS EC2

on:
  push:
    branches:
      - main
  workflow_dispatch: # Allow manual triggers from GitHub UI

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/zenigh-key
          chmod 600 ~/.ssh/zenigh-key
          ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to EC2
        env:
          EC2_HOST: ${{ secrets.EC2_HOST }}
          ENV_FILE: ${{ secrets.ENV_FILE }}
        run: |
          ssh -i ~/.ssh/zenigh-key ubuntu@$EC2_HOST << 'ENDSSH'
            set -e  # Exit on any error

            echo "📦 Starting deployment..."

            cd ~/zenigh

            # Pull latest code
            echo "🔄 Pulling latest code from main branch..."
            git pull origin main

            # Update .env file from GitHub Secrets
            echo "🔐 Updating environment variables..."
            cat > .env << 'EOF'
$ENV_FILE
EOF

            # Rebuild and restart containers
            echo "🐳 Rebuilding and restarting Docker containers..."
            docker-compose -f docker-compose.prod.yml up -d --build

            # Clean up old images to free disk space
            echo "🧹 Cleaning up old Docker images..."
            docker image prune -af

            # Show container status
            echo "✅ Deployment complete! Container status:"
            docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
          ENDSSH

      - name: Health Check
        run: |
          echo "🏥 Running health check..."
          sleep 30  # Wait for containers to fully start

          # Check if Nginx is responding
          if curl -f http://${{ secrets.EC2_HOST }} --max-time 10; then
            echo "✅ Nginx health check passed!"
          else
            echo "❌ Nginx health check failed!"
            exit 1
          fi

          # Optional: Check domain (may fail initially due to DNS propagation)
          if curl -f https://zenigh.ai --max-time 10 2>/dev/null; then
            echo "✅ Domain health check passed!"
          else
            echo "⚠️  Domain not ready yet (DNS may still be propagating)"
          fi

      - name: Deployment Summary
        if: success()
        run: |
          echo "🎉 Deployment successful!"
          echo "🌐 Website: https://zenigh.ai"
          echo "🔌 API: https://zenigh.ai/api"
          echo "📍 Direct IP: http://${{ secrets.EC2_HOST }}"

      - name: Deployment Failed
        if: failure()
        run: |
          echo "❌ Deployment failed!"
          echo "Check the logs above for details."
          exit 1
